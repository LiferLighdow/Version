<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Main Home Content -->
    <RelativeLayout
        android:id="@+id/home_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent">

        <!-- Clock and Stats Container -->
        <LinearLayout
            android:id="@+id/clock_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="80dp"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/time_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="64sp"
                android:textStyle="bold"
                android:text="00:00" />

            <TextView
                android:id="@+id/date_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:text="Monday, Jan 1"
                android:layout_marginBottom="16dp"/>

            <!-- Stats moved here -->
            <TextView
                android:id="@+id/cpu_stat"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#CCCCCC"
                android:textSize="14sp"
                android:text="CPU: --" />
            <TextView
                android:id="@+id/ram_stat"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#CCCCCC"
                android:textSize="14sp"
                android:text="RAM: --" />
            <TextView
                android:id="@+id/rom_stat"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#CCCCCC"
                android:textSize="14sp"
                android:text="ROM: --" />
        </LinearLayout>

        <!-- Search Bar -->
        <EditText
            android:id="@+id/home_search"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_above="@id/dock"
            android:layout_marginHorizontal="24dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/search_bar_background"
            android:drawableStart="@android:drawable/ic_menu_search"
            android:drawablePadding="12dp"
            android:hint="Search apps or web..."
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:paddingHorizontal="20dp"
            android:textColor="#FFFFFF"
            android:textColorHint="#CCCCCC" />

        <!-- Dock -->
        <LinearLayout
            android:id="@+id/dock"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="20dp"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageView android:id="@+id/dock_1" android:layout_width="64dp" android:layout_height="64dp" android:layout_marginHorizontal="12dp" android:background="@drawable/icon_background" android:padding="12dp" android:scaleType="fitCenter" />
            <ImageView android:id="@+id/dock_2" android:layout_width="64dp" android:layout_height="64dp" android:layout_marginHorizontal="12dp" android:background="@drawable/icon_background" android:padding="12dp" android:scaleType="fitCenter" />
            <ImageView android:id="@+id/dock_3" android:layout_width="64dp" android:layout_height="64dp" android:layout_marginHorizontal="12dp" android:background="@drawable/icon_background" android:padding="12dp" android:scaleType="fitCenter" />
            <ImageView android:id="@+id/dock_4" android:layout_width="64dp" android:layout_height="64dp" android:layout_marginHorizontal="12dp" android:background="@drawable/icon_background" android:padding="12dp" android:scaleType="fitCenter" />
        </LinearLayout>
    </RelativeLayout>

    <!-- Search Overlay (only shown when typing) -->
    <LinearLayout
        android:id="@+id/search_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#EE000000"
        android:orientation="vertical"
        android:visibility="gone"
        android:padding="20dp">

        <EditText
            android:id="@+id/search_input"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/search_bar_background"
            android:hint="Search apps or web..."
            android:textColor="#FFFFFF"
            android:textColorHint="#CCCCCC"
            android:paddingHorizontal="20dp"
            android:imeOptions="actionSearch"
            android:inputType="text" />

        <ListView
            android:id="@+id/search_results"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="12dp"
            android:divider="@null" />
    </LinearLayout>

</FrameLayout>